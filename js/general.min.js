$(".do-submitBioSig").on("click", function(a) {
    a.preventDefault(), window.location.hash = $("form").serialize();
}), $(window).on("hashchange", function() {
    var a = window.location.hash.substring(1).replace(new RegExp("=", "g"), "").split("&");
    getContent(a);
});

var getContent = function(a) {
    console.log(a);
    for (var b = [], c = {}, d = 0; d < a.length; d++) {
        var e = a[d];
        if (c[e] = !0, window.bioSigData.answers[e].content) {
            var f = window.bioSigData.content[window.bioSigData.answers[e].content];
            -1 == b.indexOf(f) && b.push(f + "<br>");
        }
        if (window.bioSigData.answers[e].pairs) {
            console.log(e + " has a pair");
            var g = window.bioSigData.answers[e].pairs;
            for (var h in g) $.each(window.bioSigData.answers[e].pairs[h], function(a, d) {
                var e = window.bioSigData.content[h];
                c[d] && -1 == b.indexOf(e) && b.push(e);
            });
        }
    }
    console.log(b);
};